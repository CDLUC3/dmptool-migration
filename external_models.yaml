# Column definitions for the external MySQL database `dmp`. We include these here to help
# deal with columns types that cannot be derived automatically by mysqlmesh.
#
# DO NOT RUN `mysqlmesh create_external_models` AS THIS WILL REPLACE THIS FILE AND BREAK THE
# DUCKDB TESTS!
#
- name: '`dmp`.`research_domains`'
  columns:
    id: BIGINT
    parent_id: BIGINT
  gateway: mysql
